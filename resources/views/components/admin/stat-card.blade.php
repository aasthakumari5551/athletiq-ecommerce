@props(['label', 'value'])

<div class="border border-gray-200 bg-white p-5">
    <p class="text-xs font-black uppercase text-gray-400">{{ $label }}</p>
    <p class="mt-3 text-3xl font-black uppercase text-black">{{ $value }}</p>
</div>
